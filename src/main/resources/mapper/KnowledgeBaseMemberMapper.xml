<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.serching.fulltextsearching.mapper.KnowledgeBaseMemberMapper">

    <!-- 批量插入 -->
    <insert id="insertBatch">
        INSERT INTO t_knowledge_base_member (kb_id, user_id, member_type, created_at, updated_at)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.kbId}, #{item.userId}, #{item.memberType}, NOW(), NOW())
        </foreach>
    </insert>

</mapper>